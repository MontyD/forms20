extends layouts/base
block content
  body
    nav.top.absolute
      .container
        include ./includes/_headerLinks.pug
    header.slightGradient
      h1.shadow Oops! #{error.status}
      p.white= message

    main.container.center
      pre #{error.stack}
