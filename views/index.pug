extends layouts/base
block content
  body
    nav.top.absolute
      .container
        include ./includes/_headerLinks.pug
    header.slightGradient
      h1.shadow Forms Twenty
      if (sessionForm)
        a(href="/temporaryForms?clearsession=true", title="Create New Form").fakeButton.accent Create New Form
        a(href="/temporaryForms", title="Continue form").fakeButton Continue Form
      else
        a(href="/temporaryForms", title="Create New Form").fakeButton.accent Create Form
